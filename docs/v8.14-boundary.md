# Agent Space v8.14 Boundary

状态：complete
目标：Human Gateway exposes the local requester registry as a read-only API and table.

## v8.14 新增

- Human Gateway serves `GET /api/requester/registry`.
- The endpoint returns the existing local requester registry JSON shape:
  - `zone`
  - `revocations`
  - `agents[]`
- The Human Gateway page renders a `Requester Registry` table.
- The table shows each registered requester alias, `aid`, and Zone binding id.
- The registry table reflects multiple requester aliases persisted by v8.13.

## 为什么这是 v8.14

v8.13 made the local requester registry preserve multiple aliases.

v8.14 makes that registry inspectable:

```text
rebinding writes registry agents[]
  -> GET /api/requester/registry
  -> Human Gateway Requester Registry table
```

This is the honest step before requester selection: humans can see which requester aliases are recognized, but the browser still owns signing keys.

## v8.14 不新增

- 不做 browser multi-key manager。
- 不做 Human Gateway requester selector UI。
- 不做 alias delete/disable。
- 不做 registry mutation API。
- 不做 server-side browser private-key custody。
- 不做 login/session identity。
- 不做 public deployment。

## 验收

```bash
go test ./...
node --test --test-concurrency=1 go-fed-discovery.test.mjs
node --test --test-concurrency=1 *.test.mjs
```

The Go integration test must fail if `GET /api/requester/registry` does not expose multiple requester aliases or if the Human Gateway page does not render the `Requester Registry` table.
