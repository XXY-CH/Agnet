# Agent Space v10.23 Boundary

状态：complete
目标：Prove the local public-listen node can answer a capability query.

## v10.23 新增

- `scripts/public-node-proof.mjs` now sends authenticated `FED_QUERY` for `summarize.text`.
- The proof output includes:
  - `query_capability: "summarize.text"`
  - `query_match_count: 1`
  - `query_status: "active"`
- The existing authenticated `FED_RESOLVE` proof remains.

## v10.23 不新增

- 不做公网可达性证明。
- 不做 NAT / relay。
- 不做 hosted public node。
- 不做 TLS certificate issuance。
- 不做 QUIC。
- 不做 deployment automation。
- 不做 broad protocol probe suite beyond `FED_RESOLVE` and `FED_QUERY`。

## 验收

```bash
node --test --test-concurrency=1 public-node-proof.test.mjs
node --test --test-concurrency=1 go-fed-discovery.test.mjs
go test ./...
node --test --test-concurrency=1 *.test.mjs
git diff --check
```
