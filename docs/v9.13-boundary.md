# Agent Space v9.13 Boundary

状态：complete
目标：unsupported sandbox claims fail before Human Gateway approval.

## v9.13 新增

- `executeTask` validates `sandbox_claim` immediately after `task.accepted`.
- Unsupported `container-namespace` claims no longer emit `approval.required`.
- Unsupported sandbox claims no longer create Human Gateway approval state.
- Failed task state still records `sandbox_probe`.

## 为什么这是 v9.13

Ultimate trust requires approving tasks only after the runtime can honestly satisfy the claimed isolation boundary.
v9.13 moves the existing sandbox claim gate before approval, so a human is not asked to approve an impossible container-isolated execution.

## v9.13 不新增

- 不实现 container namespace sandbox。
- 不做 Docker/OCI runtime。
- 不做 Linux namespace setup。
- 不做 seccomp/AppArmor/profile enforcement。
- 不做 network namespace。
- 不做 filesystem mount namespace。
- 不做 cgroup limits。
- 不做 remote attestation。
- 不做 scheduler or automatic drain。
- 不做 A2A/ARD compatibility。

## 验收

```bash
go test ./...
node --test --test-concurrency=1 test/go-fed-discovery.test.mjs
node --test --test-concurrency=1 test/*.test.mjs
git diff --check
```

The integration test must fail if an unsupported `container-namespace` claim emits `approval.required` or leaves approval state behind.
