# Agent Space v9.11 Boundary

状态：complete
目标：unsupported container sandbox claims persist runtime probe evidence before tool execution.

## v9.11 新增

- Sandbox claim preflight returns a structured sandbox claim error.
- Failed task state records `sandbox_probe` for unsupported `container-namespace` claims.
- The probe records `claim`, `supported: false`, and a reason that the container namespace runtime is not implemented.
- Integration coverage proves the claimed container worker still does not start its external marker tool.

## 为什么这是 v9.11

Ultimate needs real sandbox isolation and trustworthy proof, but the current runtime cannot honestly claim container namespace isolation yet.
v9.11 makes that gap explicit and durable in task state, so unsupported isolation claims become auditable runtime capability evidence instead of a plain string failure.

## v9.11 不新增

- 不实现 container namespace sandbox。
- 不做 Docker/OCI runtime。
- 不做 Linux namespace setup。
- 不做 seccomp/AppArmor/profile enforcement。
- 不做 network namespace。
- 不做 filesystem mount namespace。
- 不做 cgroup limits。
- 不做 remote attestation。
- 不做 scheduler or automatic drain。
- 不做 A2A/ARD compatibility。

## 验收

```bash
go test ./...
node --test --test-concurrency=1 test/go-fed-discovery.test.mjs
node --test --test-concurrency=1 test/*.test.mjs
git diff --check
```

The integration test must fail if an unsupported `container-namespace` claim writes failed task state without `sandbox_probe`.
