# Agent Space v12.13 Boundary

状态：complete
目标：Make `proof-bundle` reject non-federation transport proofs.

## v12.13 新增

- `asp-verify.mjs proof-bundle <bundle.json>` now requires the verified signed receipt `transport_proof.transport` to be `fed+tcp`.
- A bundle whose signed receipt transport proof uses `asp+local` while keeping `public_transport: true` is rejected with `bundle transport_proof invalid`.
- `test/public-node-proof.test.mjs` proves this by re-signing a receipt with local transport and matching the bundle fields.

## 为什么这是 v12.13

v12.12 made transport proof fields complete, but `proof: "public-node-proof"` still needed to reject a local-process transport label. This keeps the public proof tied to the current federation TCP listener without claiming remote-host reachability.

## v12.13 不新增

- 不实现 external public reachability proof。
- 不实现 hosted public node。
- 不增加 QUIC, TLS, DNS, NAT traversal, or remote probe infrastructure。
- 不改变 normal `fed-receipt` verification。
- 不改变 verifier JSON output。
- 不实现 transport negotiation。
- 不实现 batch verifier。
- 不实现 JSON Schema。
- 不实现 generic proof bundle schema。
- 不实现 package signing or SBOM。
- 不实现 A2A/ARD compatibility。
- 不实现 scheduler-owned routing。

It does not prove reachability from another host.

## 验收

```bash
node --test --test-concurrency=1 test/public-node-proof.test.mjs
node --test --test-concurrency=1 test/docs-contract.test.mjs
gofmt -l . && git diff --check
go test ./...
node --test --test-concurrency=1 test/*.test.mjs
```
