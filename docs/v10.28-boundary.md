# Agent Space v10.28 Boundary

状态：complete
目标：Prove public-listen artifact reads reject out-of-receipt artifact URIs.

## v10.28 新增

- `scripts/public-node-proof.mjs` sends a negative authenticated `FED_ARTIFACT_READ` for an artifact URI that is not in the signed receipt.
- The gateway returns a `FED_TASK_ERROR` from the existing receipt-bound artifact check.
- The proof output includes:
  - `artifact_reject: true`
  - `artifact_reject_error` matching `receipt artifact not found`
- `public-node-proof.test.mjs` asserts the rejection evidence.

## v10.28 不新增

- 不做公网可达性证明。
- 不做 NAT / relay。
- 不做 hosted public node。
- 不做 TLS certificate issuance。
- 不做 QUIC。
- 不做 deployment automation。
- 不做 remote audit sync。
- 不做 audit search/index。
- 不做 general artifact service。
- 不做 range/cache/object-store fetch。
- 不做 artifact authorization model beyond receipt-bound URI checks.

## 验收

```bash
node --test --test-concurrency=1 public-node-proof.test.mjs
node --test --test-concurrency=1 go-fed-discovery.test.mjs
go test ./...
node --test --test-concurrency=1 *.test.mjs
bash scripts/docker-proof-demo.sh
git diff --check
```
