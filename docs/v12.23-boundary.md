# Agent Space v12.23 Boundary

状态：complete
目标：Make the package proof verifier reject non-object package proof manifests before reading proof fields.

## v12.23 新增

- `asp-verify.mjs package-proof <manifest.json>` rejects `null` and array manifests with `package proof manifest invalid`.
- `package-contract.test.mjs` covers both non-object package proof manifest shapes.

## 为什么这是 v12.23

v12.22 added the package proof verifier command, but malformed non-object inputs could leak JavaScript field-read errors. v12.23 adds the smallest fail-closed object gate for that verifier path.

## v12.23 不新增

- 不实现 package signing。
- 不实现 SBOM。
- 不发布 npm package。
- 不改变 `package.json` exports/bin/files。
- 不改变 npm `shasum` or `integrity` handling。
- 不改变 tarball SHA-256 calculation。
- 不实现 relocatable package proof format。
- 不实现 external public reachability proof。
- 不实现 hosted public node。
- 不增加 DNS, TLS, QUIC, NAT traversal, or remote probe infrastructure。
- 不改变 normal `fed-receipt` verification。
- 不改变 `proof-bundle` verifier JSON output。
- 不实现 transport negotiation。
- 不实现 batch verifier。
- 不实现 JSON Schema。
- 不实现 generic proof bundle schema。
- 不实现 A2A/ARD compatibility。
- 不实现 scheduler-owned routing。

It does not sign the tarball or produce an SBOM.

## 验收

```bash
node --test --test-concurrency=1 package-contract.test.mjs
node --test --test-concurrency=1 docs-contract.test.mjs
gofmt -l . && git diff --check
go test ./...
node --test --test-concurrency=1 *.test.mjs
```
