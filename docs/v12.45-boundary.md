# Agent Space v12.45 Boundary

状态：complete
目标：Close v12 as the externally consumable proof-surface milestone without adding another runtime surface.

## v12.45 新增

- README marks v12 closed at `v12.45-protocol`.
- `docs/v12-roadmap.md` marks v12 closed and records this closeout slice.
- `docs/asp-core-draft.md` points to the current closed prototype tag.
- `docs/implementation-status.md` names the closed v12 baseline and summarizes the closed v12 proof surface.
- `test/docs-contract.test.mjs` guards the public closeout wording.

## 为什么这是 v12.45

v12.44 closed the remaining local package-proof signer authorization gap. The remaining v12 roadmap candidates require external hosted-node evidence, release transparency/SBOM decisions, compatibility work, or the upper-layer demo that will be developed separately. Adding another local-only verifier field now would stretch v12 past its proof-surface purpose.

## v12.45 不新增

- 不实现新的协议字段。
- 不实现新的 verifier CLI。
- 不实现 hosted public node。
- 不实现 real outside-host reachability。
- 不实现 npm registry signing。
- 不实现 release transparency。
- 不实现 SBOM。
- 不实现 package publish。
- 不实现 scheduler-owned routing。
- 不实现 upper-layer demo/master-agent orchestration。
- 不实现 A2A/ARD compatibility。

## 验收

```bash
node --test --test-concurrency=1 test/docs-contract.test.mjs
gofmt -l . && git diff --check
go test ./...
node --test --test-concurrency=1 test/*.test.mjs
```

v12 到此收尾。v13+ 应继续从 Ultimate 里选窄边界，而不是把 hosted deployment, release transparency, scheduler, compatibility, economy, or upper-layer orchestration 塞进当前 proof surface。
