# Agent Space v10.45 Boundary

状态：complete
目标：Make receipt-artifact verifier outputs and proof summaries expose the verified artifact byte digests.

## v10.45 新增

- `asp-verify.mjs fed-receipt-artifacts` includes `artifact_sha256s` on success.
- `scripts/proof-demo.sh` forwards `artifact_sha256s` in its summary JSON.
- `scripts/public-node-proof.mjs` forwards `artifact_sha256s` in its summary JSON.
- Vector, proof demo, and public-node tests assert the digest list matches the verified artifact manifest SHA-256.

## v10.45 不新增

- 不做 receipt store/search。
- 不做 artifact search。
- 不做 batch verification。
- 不做 HTTP verifier service。
- 不做 remote artifact fetch。
- 不做新的 frame schema。
- 不做 package signing。
- 不做 SBOM。
- 不做公网可达性证明。
- 不做 hosted public node。

## 验收

```bash
node --test --test-concurrency=1 test-vectors.test.mjs proof-demo.test.mjs public-node-proof.test.mjs
node --test --test-concurrency=1 *.test.mjs
go test ./...
git diff --check
gofmt -l .
```
