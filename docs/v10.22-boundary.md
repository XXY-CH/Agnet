# Agent Space v10.22 Boundary

状态：complete
目标：Prove the local public-listen node can serve one authenticated federation frame.

## v10.22 新增

- `scripts/public-node-proof.mjs` now creates a trusted proof origin Zone.
- The proof helper connects to the public-listen gateway over TCP.
- It completes `HELLO` / `AUTH`.
- It sends `FED_RESOLVE` for `agent://zone-b/summarizer`.
- The proof output includes:
  - `resolve_alias: "agent://zone-b/summarizer"`
  - `resolve_close: true`

## v10.22 不新增

- 不做公网可达性证明。
- 不做 NAT / relay。
- 不做 hosted public node。
- 不做 TLS certificate issuance。
- 不做 QUIC。
- 不做 deployment automation。
- 不做 broad protocol probe suite beyond one `FED_RESOLVE` round trip。

## 验收

```bash
node --test --test-concurrency=1 public-node-proof.test.mjs
node --test --test-concurrency=1 go-fed-discovery.test.mjs
go test ./...
node --test --test-concurrency=1 *.test.mjs
git diff --check
```
