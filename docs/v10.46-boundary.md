# Agent Space v10.46 Boundary

状态：complete
目标：Make receipt-artifact verifier outputs and proof summaries expose the verified artifact manifest hashes.

## v10.46 新增

- `asp-verify.mjs fed-receipt-artifacts` includes `artifact_manifest_hashes` on success.
- `scripts/proof-demo.sh` forwards `artifact_manifest_hashes` in its summary JSON.
- `scripts/public-node-proof.mjs` forwards `artifact_manifest_hashes` in its summary JSON.
- Vector, proof demo, and public-node tests assert the hash list matches the verified artifact manifest metadata hash.

## v10.46 不新增

- 不做 receipt store/search。
- 不做 artifact search。
- 不做 batch verification。
- 不做 HTTP verifier service。
- 不做 remote artifact fetch。
- 不做新的 manifest/frame schema。
- 不做 package signing。
- 不做 SBOM。
- 不做公网可达性证明。
- 不做 hosted public node。

## 验收

```bash
node --test --test-concurrency=1 test-vectors.test.mjs proof-demo.test.mjs public-node-proof.test.mjs
node --test --test-concurrency=1 *.test.mjs
go test ./...
git diff --check
gofmt -l .
```
