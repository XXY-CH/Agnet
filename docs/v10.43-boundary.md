# Agent Space v10.43 Boundary

状态：complete
目标：Make proof summary JSON outputs expose the artifact count verified by the existing receipt-artifact CLI.

## v10.43 新增

- `scripts/proof-demo.sh` includes top-level `artifact_count` in its summary JSON.
- `scripts/public-node-proof.mjs` includes top-level `artifact_count` in its summary JSON.
- Both scripts reuse `asp-verify.mjs fed-receipt-artifacts` output instead of counting artifacts themselves.
- Proof demo and public-node tests assert the summary artifact count is `1` for the current proof fixtures.

## v10.43 不新增

- 不做 receipt store/search。
- 不做 batch verification。
- 不做 HTTP verifier service。
- 不做新的 frame schema。
- 不做 package signing。
- 不做 SBOM。
- 不做公网可达性证明。
- 不做 hosted public node。

## 验收

```bash
node --test --test-concurrency=1 proof-demo.test.mjs public-node-proof.test.mjs
node --test --test-concurrency=1 *.test.mjs
go test ./...
git diff --check
gofmt -l .
```
