# Agent Space v10.47 Boundary

状态：complete
目标：Close v10 as an externally verifiable proof/accountability milestone without adding another runtime surface.

## v10.47 新增

- README marks v10 closed at `v10.47-protocol`.
- `docs/v10-roadmap.md` marks v10 closed and records this closeout slice.
- `docs/asp-core-draft.md` points to the current closed prototype tag.
- `docs/implementation-status.md` names the closed v10 baseline.
- `docs-contract.test.mjs` guards the public closeout wording.

## 为什么这是 v10.47

v10.46 finished the last proof-summary evidence field: verified artifact manifest hashes. Adding another proof-summary field now would be weaker than closing the milestone. v10.47 makes the public docs and contract tests agree that v10 is done, so the next work can start cleanly as v11+ instead of quietly stretching v10.

## v10.47 不新增

- 不做新的协议字段。
- 不做新的 verifier CLI。
- 不做 receipt store/search。
- 不做 batch verification。
- 不做 HTTP verifier service。
- 不做 package signing。
- 不做 SBOM。
- 不做公网可达性证明。
- 不做 hosted public node。
- 不做 dynamic Swarm decomposition。
- 不做 scheduler-owned DAG execution。
- 不做 parallel Swarm execution。
- 不做 cross-Zone Swarm。
- 不做 A2A/ARD compatibility。

## 验收

```bash
node --test --test-concurrency=1 docs-contract.test.mjs
node --test --test-concurrency=1 *.test.mjs
go test ./...
git diff --check
gofmt -l .
```

v10 到此收尾。v11+ 应从 Ultimate 里继续选窄边界，而不是把 public network、scheduler、dynamic Swarm、economy 或 compatibility 一次性塞进当前 proof layer。
