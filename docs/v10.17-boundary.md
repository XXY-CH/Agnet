# Agent Space v10.17 Boundary

状态：complete
目标：Reject Swarm close proofs that close a Swarm with no same-audit receipts.

## v10.17 新增

- `--verify-audit` rejects a `go_swarm_close` proof when the referenced `swarm_id` has no completed step receipts earlier in the same audit.
- Empty `step_receipts` can no longer close an audit-absent Swarm.
- Existing close proof signature, duplicate-close, completeness, duplicate-step, ordering, task id, and receipt digest checks still apply.
- `cmd/go-fed-discovery/main_test.go` proves unknown empty Swarm close proofs are rejected.

## 为什么这是 v10.17

v10.16 made one same-audit Swarm id close only once. v10.17 closes the next proof hole: a close proof must close real same-audit work, not a Swarm id that never appeared in the audit.

## v10.17 不新增

- 不做 dynamic Swarm decomposition。
- 不做 scheduler-owned DAG execution。
- 不做 parallel Swarm execution。
- 不做 conflict/merge receipts。
- 不做 cross-Zone Swarm。
- 不做 Swarm UI。
- 不做 receipt store/search。
- 不做 A2A/ANP/AGNTCY compatibility。
- 不做 cross-audit Swarm lifecycle storage。

## 验收

```bash
go test ./cmd/go-fed-discovery -run TestVerifyAuditRejectsSwarmInputArtifactMismatch -count=1
node --test --test-concurrency=1 go-fed-discovery.test.mjs
go test ./...
node --test --test-concurrency=1 *.test.mjs
git diff --check
```

The focused Go test must fail before `--verify-audit` rejects empty close proofs for a Swarm id absent from the audit.
