# Agent Space v8.48 Boundary

状态：complete
目标：Human Gateway receipt rows can load completed task transcript streams into a local viewer.

## v8.48 新增

- Receipt rows with `sandbox.tool_transcript_ref` render a `stream transcript` button.
- The page includes a `transcript-viewer` panel.
- `loadTranscript(taskID)` fetches `/api/transcripts/stream?task_id=<id>`.
- The viewer displays the returned NDJSON proof stream text.
- The Go integration test checks the viewer, button, and stream API fetch wiring.

## 为什么这是 v8.48

v8.47 exposed task-scoped transcript streams as an API.
v8.48 makes that proof stream reachable from the Human Gateway control plane:

```text
receipt row
  -> stream transcript button
  -> /api/transcripts/stream?task_id=<id>
  -> transcript-viewer
```

This keeps the completed-transcript path product-visible without adding a new transport protocol.

## v8.48 不新增

- 不做 live tail of running subprocess stdout。
- 不做 SSE/WebSocket transcript protocol。
- 不做 transcript search/index。
- 不做 transcript parsing/rendering beyond plain text。
- 不做 object-store backend。
- 不做 artifact GC。
- 不做 auth model for transcript reads。
- 不做 container namespace sandbox。
- 不做 public deployment。
- 不做 A2A/ARD compatibility。

## 验收

```bash
go test ./...
node --test --test-concurrency=1 go-fed-discovery.test.mjs
node --test --test-concurrency=1 *.test.mjs
```

The Go integration test must fail if the Human Gateway page stops exposing the transcript viewer or stream fetch wiring.
