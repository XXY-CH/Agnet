# Agent Space v12.18 Boundary

状态：complete
目标：Make the npm-facing verifier surface produce a real local package artifact with npm-owned digest metadata.

## v12.18 新增

- `scripts/package-proof.mjs` runs `npm pack --json --pack-destination state/package-proof`.
- The script returns `package_proof: "ok"`, package name/version, tarball path, size, unpacked size, SHA-1 shasum, SHA-512 integrity, and packaged file list.
- `test/package-contract.test.mjs` covers tarball creation and the expected package file list.

## 为什么这是 v12.18

v12 has made proof outputs easier to consume, but package signing or SBOM work needs an actual release artifact to bind. v12.18 creates that local artifact first and records npm-owned digest metadata without choosing a signing or SBOM format.

## v12.18 不新增

- 不实现 package signing。
- 不实现 SBOM。
- 不发布 npm package。
- 不改变 `package.json` exports/bin/files。
- 不实现 external public reachability proof。
- 不实现 hosted public node。
- 不增加 DNS, TLS, QUIC, NAT traversal, or remote probe infrastructure。
- 不改变 normal `fed-receipt` verification。
- 不改变 `proof-bundle` verifier JSON output。
- 不实现 transport negotiation。
- 不实现 batch verifier。
- 不实现 JSON Schema。
- 不实现 generic proof bundle schema。
- 不实现 A2A/ARD compatibility。
- 不实现 scheduler-owned routing。

It does not sign the tarball or produce an SBOM.

## 验收

```bash
node --test --test-concurrency=1 test/package-contract.test.mjs
node --test --test-concurrency=1 test/docs-contract.test.mjs
gofmt -l . && git diff --check
go test ./...
node --test --test-concurrency=1 test/*.test.mjs
```
