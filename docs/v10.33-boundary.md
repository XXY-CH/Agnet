# Agent Space v10.33 Boundary

状态：complete
目标：Make Docker proof demo base images overrideable without adding registry, publishing, or deployment machinery.

## v10.33 新增

- `Dockerfile` declares `AGNET_NODE_BASE_IMAGE` with default `node:22-bookworm-slim`.
- `Dockerfile.public-node-proof` declares `AGNET_GO_BASE_IMAGE` with default `golang:1.26.1-bookworm`.
- `Dockerfile.public-node-proof` declares `AGNET_NODE_BASE_IMAGE` with default `node:22-bookworm-slim`.
- `scripts/docker-proof-demo.sh` passes `AGNET_NODE_BASE_IMAGE` as a Docker build argument, preserving the existing default.
- `scripts/docker-public-node-proof.sh` passes `AGNET_GO_BASE_IMAGE` and `AGNET_NODE_BASE_IMAGE` as Docker build arguments, preserving the existing defaults.
- `docker-demo.test.mjs` guards the override contract for both Docker proof wrappers.

## v10.33 不新增

- 不做 Docker registry mirror 配置。
- 不做 Docker image publishing。
- 不做 Docker Compose。
- 不做 hosted public node。
- 不做公网可达性证明。
- 不做 package signing。
- 不做 SBOM。
- 不做 supply-chain attestation。
- 不做 container namespace sandbox。
- 不改变 proof demo 输出语义。

## 用法

```bash
AGNET_NODE_BASE_IMAGE=node:22-bookworm-slim bash scripts/docker-proof-demo.sh
AGNET_GO_BASE_IMAGE=golang:1.26.1-bookworm AGNET_NODE_BASE_IMAGE=node:22-bookworm-slim bash scripts/docker-public-node-proof.sh
```

## 验收

```bash
node --test --test-concurrency=1 docker-demo.test.mjs
AGNET_NODE_BASE_IMAGE=node:22-bookworm-slim bash scripts/docker-proof-demo.sh
AGNET_GO_BASE_IMAGE=golang:1.26.1-bookworm AGNET_NODE_BASE_IMAGE=node:22-bookworm-slim bash scripts/docker-public-node-proof.sh
node --test --test-concurrency=1 public-node-proof.test.mjs
node --test --test-concurrency=1 go-fed-discovery.test.mjs
go test ./...
node --test --test-concurrency=1 *.test.mjs
git diff --check
```
