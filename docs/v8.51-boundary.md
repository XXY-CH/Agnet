# Agent Space v8.51 Boundary

状态：complete
目标：Human Gateway live transcript loading polls the running snapshot in the existing local transcript viewer.

## v8.51 新增

- `loadLiveTranscript(taskID)` refreshes the live snapshot immediately.
- Live transcript loading polls `/api/transcripts/live?task_id=<id>` every second.
- Loading a completed transcript clears the live transcript poller.
- The Go integration test checks the running page exposes polling and clear-poller wiring before cancellation.

## 为什么这是 v8.51

v8.49 exposed running stdout snapshots and v8.50 made them reachable from the Human Gateway page.
v8.51 turns the manual load into a tiny polling UI without changing the protocol:

```text
live transcript button
  -> immediate snapshot load
  -> setInterval refresh
  -> clear polling when completed transcript is selected
```

## v8.51 不新增

- 不做 SSE/WebSocket continuous tail。
- 不做 server-side push。
- 不做 MCP live transcript snapshots。
- 不做 signed receipt proof for incomplete running output。
- 不做 transcript search/index。
- 不做 object-store backend。
- 不做 artifact GC。
- 不做 auth model for transcript reads。
- 不做 container namespace sandbox。
- 不做 public deployment。
- 不做 A2A/ARD compatibility。

## 验收

```bash
go test ./...
node --test --test-concurrency=1 go-fed-discovery.test.mjs
node --test --test-concurrency=1 *.test.mjs
```

The Go integration test must fail if the live transcript UI stops polling the running snapshot or stops clearing the poller when a completed transcript is selected.
