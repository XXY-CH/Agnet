# Agent Space v10.41 Boundary

状态：complete
目标：Make proof summary JSON outputs carry the verifier CLI receipt digest they already depend on.

## v10.41 新增

- `scripts/proof-demo.sh` includes top-level `receipt_digest` in its summary JSON.
- `scripts/public-node-proof.mjs` includes top-level `receipt_digest` in its summary JSON.
- Both scripts reuse `asp-verify.mjs fed-receipt-artifacts` output instead of recalculating the digest.
- Proof demo and public-node tests assert the summary digest matches `sha256(canonical(receipt body without signature))`.

## v10.41 不新增

- 不做 receipt store/search。
- 不做 batch verification。
- 不做 HTTP verifier service。
- 不做新的 frame schema。
- 不做 package signing。
- 不做 SBOM。
- 不做公网可达性证明。
- 不做 hosted public node。

## 验收

```bash
node --test --test-concurrency=1 test/proof-demo.test.mjs test/public-node-proof.test.mjs
node --test --test-concurrency=1 test/*.test.mjs
go test ./...
git diff --check
gofmt -l .
```
