# Agent Space v10.36 Boundary

状态：complete
目标：Make the public-listen Swarm close proof frame carry the trusted Zone material needed to verify its signature out of band.

## v10.36 新增

- `scripts/public-node-proof.mjs` writes `state/public-node-proof-swarm-close-trusted-zones.json`.
- The proof output includes `swarm_close_trusted_zones` with that path.
- `public-node-proof.test.mjs` checks the trusted Zone file matches the close frame Zone.

## v10.36 不新增

- 不做新的 Swarm verifier CLI。
- 不做 Swarm receipt store/search。
- 不做新的 Swarm frame schema。
- 不做 dynamic Swarm decomposition。
- 不做 scheduler-owned DAG execution。
- 不做 parallel Swarm execution。
- 不做 cross-Zone Swarm。
- 不做公网可达性证明。
- 不做 hosted public node。

## 验收

```bash
node --test --test-concurrency=1 public-node-proof.test.mjs
bash scripts/public-node-proof.sh
node --test --test-concurrency=1 go-fed-discovery.test.mjs
go test ./...
node --test --test-concurrency=1 *.test.mjs
git diff --check
```
