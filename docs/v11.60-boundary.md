# Agent Space v11.60 Boundary

状态：complete
目标：Make Go receipt and audit artifact manifest verification reject non-string manifest URIs.

## v11.60 新增

- Go reusable receipt verification rejects non-string artifact manifest `uri` values with `artifact manifest uri invalid`.
- Go audit artifact verification rejects non-string artifact manifest `uri` values with `artifact manifest uri invalid` before comparing artifact refs or reading local bytes.
- The fix stays on the artifact manifest proof input surface: URI must be a string, but this slice does not change local URI/path validation and does not add full artifact manifest schema validation.

## 为什么这是 v11.60

Go artifact manifest verification already rejects malformed `media_type`, `manifest_hash`, `sha256`, `size`, and list entries before accepting signed artifact metadata. A non-string manifest `uri` previously failed only as an indirect ref mismatch, which left the proof boundary less explicit than the rest of the manifest metadata.

## v11.60 不新增

- 不改变 artifact manifest metadata schema。
- 不实现 generic JSON Schema validation。
- 不改变 receipt artifact manifest comparison semantics。
- 不实现 remote artifact fetch。
- 不实现 object-store artifact backend。
- 不实现 artifact retention policy。
- 不实现 scheduler-owned routing。
- 不实现 dynamic Swarm decomposition。
- 不实现 A2A/ARD compatibility。

## 验收

```bash
go test ./verifier -run 'TestArtifactManifestRejectsMalformed(URI|MediaType|ManifestHash|Size|ListEntries)' -count=1
go test ./cmd/go-fed-discovery -run 'TestArtifactManifestRejectsMalformed(URI|MediaType|ManifestHash|Size|ListEntries)BeforeByteChecks' -count=1
node --test --test-concurrency=1 test/docs-contract.test.mjs
go test ./...
node --test --test-concurrency=1 test/*.test.mjs
git diff --check
gofmt -l .
```
