# Agent Space v10.21 Boundary

状态：complete
目标：Add a local public-node proof path.

## v10.21 新增

- `scripts/public-node-proof.sh` builds the Go gateway and runs a proof helper.
- `scripts/public-node-proof.mjs` starts the gateway with `--listen-host 0.0.0.0`.
- The proof output includes:
  - `public_node_proof: "ok"`
  - `listen_host: "0.0.0.0"`
  - `public_transport: true`
  - `transport: "fed+tcp"`
- `test/public-node-proof.test.mjs` runs the proof script end to end.

## v10.21 不新增

- 不做公网可达性证明。
- 不做 NAT / relay。
- 不做 hosted public node。
- 不做 TLS certificate issuance。
- 不做 QUIC。
- 不做 deployment automation。

## 验收

```bash
node --test --test-concurrency=1 test/public-node-proof.test.mjs
node --test --test-concurrency=1 test/go-fed-discovery.test.mjs
go test ./...
node --test --test-concurrency=1 test/*.test.mjs
git diff --check
```
