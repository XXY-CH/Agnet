# Agent Space v8.46 Boundary

状态：complete
目标：Task-scoped artifact manifest proofs support HEAD proof headers without returning JSON bytes.

## v8.46 新增

- `HEAD /api/artifacts/manifest?task_id=<id>&uri=<artifact-uri>` runs the same task-scoped receipt and artifact verification path as GET.
- The response returns `X-Agent-Space-Audit-Hash`.
- The response returns `X-Agent-Space-Receipt-Digest`.
- The response returns `X-Agent-Space-Artifact-SHA256`.
- The response returns `X-Agent-Space-Artifact-Manifest-Hash`.
- The Go integration test checks the headers and empty body.

## 为什么这是 v8.46

v8.45 made task-scoped manifest proof JSON available.
v8.46 makes that proof inspectable without downloading the JSON body:

```text
receipt task_id + artifact uri
  -> signed receipt manifest
  -> HEAD proof headers
```

This mirrors the earlier artifact read HEAD proof without adding a cache, browser, or object store.

## v8.46 不新增

- 不做 URI-only manifest HEAD。
- 不做 audit index。
- 不做 audit viewer。
- 不改 receipt schema。
- 不做 receipt store/index。
- 不做 artifact browser。
- 不做 transcript browser。
- 不做 remote artifact fetch/download。
- 不做 object-store backend。
- 不做 artifact GC。
- 不做 auth model for artifact reads。
- 不做 HTTP cache protocol。
- 不做 range requests。
- 不做 streamed transcript UI。
- 不做 container namespace sandbox。
- 不做 public deployment。
- 不做 A2A/ARD compatibility。

## 验收

```bash
go test ./...
node --test --test-concurrency=1 go-fed-discovery.test.mjs
node --test --test-concurrency=1 *.test.mjs
```

The Go integration test must fail if task-scoped artifact manifest HEAD stops returning proof headers or returns a body.
