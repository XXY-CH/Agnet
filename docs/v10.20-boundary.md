# Agent Space v10.20 Boundary

状态：complete
目标：Publish verified Docker proof demo output.

## v10.20 新增

- `bash scripts/docker-proof-demo.sh` was verified against Docker Server `29.0.1`.
- The Docker build produced `agnet-proof-demo:latest`.
- The container proof output returned:
  - `proof_demo: "ok"`
  - `artifact_verify: "ok"`
  - `fed_receipt_artifacts_verify: "ok"`
  - `receipt_frame: "state/proof-demo-fed-receipt.json"`
  - `trusted_zones: "state/proof-demo-trusted-zones.json"`

## v10.20 不新增

- 不做 public node。
- 不做 hosted demo。
- 不做 image publishing。
- 不做 supply-chain attestation。
- 不做 container namespace sandbox。

## 验收

```bash
docker info --format '{{json .ServerVersion}}'
bash scripts/docker-proof-demo.sh
node --test --test-concurrency=1 docs-contract.test.mjs docker-demo.test.mjs proof-demo.test.mjs
git diff --check
```
