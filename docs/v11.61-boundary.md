# Agent Space v11.61 Boundary

状态：complete
目标：Make Go receipt and audit artifact manifest verification reject present non-string AFP values.

## v11.61 新增

- Go reusable receipt verification rejects present non-string artifact manifest `afp` values with `artifact manifest afp invalid`.
- Go audit artifact verification rejects present non-string artifact manifest `afp` values with `artifact manifest afp invalid` before comparing AFP strings or reading local bytes.
- Existing string AFP mismatch behavior remains `artifact manifest afp mismatch`.
- The fix stays on the optional artifact manifest proof metadata surface: AFP is validated when present, but this slice does not make it required and does not add full artifact manifest schema validation.

## 为什么这是 v11.61

Go artifact manifest verification already rejects malformed `uri`, `media_type`, `manifest_hash`, `sha256`, `size`, and list entries before accepting signed artifact metadata. A non-string manifest `afp` previously failed only as a stringified mismatch, which left the optional AFP proof metadata boundary less explicit than the mirror-index AFP boundary.

## v11.61 不新增

- 不改变 artifact manifest metadata schema。
- 不实现 generic JSON Schema validation。
- 不改变 receipt artifact manifest comparison semantics。
- 不实现 remote artifact fetch。
- 不实现 object-store artifact backend。
- 不实现 artifact retention policy。
- 不实现 scheduler-owned routing。
- 不实现 dynamic Swarm decomposition。
- 不实现 A2A/ARD compatibility。

## 验收

```bash
go test ./verifier -run 'TestArtifactManifestRejects(MismatchedAFP|MalformedAFP|MalformedURI|MalformedMediaType|MalformedManifestHash|MalformedSize|MalformedListEntries)' -count=1
go test ./cmd/go-fed-discovery -run 'TestArtifactManifestRejects(MismatchedAFPBeforeByteChecks|MalformedAFPBeforeByteChecks|MalformedURIBeforeByteChecks|MalformedMediaTypeBeforeByteChecks|MalformedManifestHashBeforeByteChecks|MalformedSizeBeforeByteChecks|MalformedListEntriesBeforeByteChecks)' -count=1
node --test --test-concurrency=1 docs-contract.test.mjs
go test ./...
node --test --test-concurrency=1 *.test.mjs
git diff --check
gofmt -l .
```
