# Agent Space v11.63 Boundary

状态：complete
目标：Make Go audit-backed Swarm dependency verification reject malformed dependency list entries instead of silently filtering them.

## v11.63 新增

- Go `verifySwarmReceiptDependencies` rejects non-string entries in signed Swarm `after` lists with `swarm after invalid`.
- Go `verifySwarmReceiptDependencies` rejects non-object entries in signed Swarm `input_artifacts` lists with `swarm input artifact invalid`.
- Existing dependency count, step, URI, digest, manifest hash, receipt digest, duplicate step, NUL identity, and close proof checks stay unchanged.
- The fix stays on the signed Swarm dependency proof surface: dependency lists are validated before count and digest checks, but this slice does not add generic Swarm schema validation.

## 为什么这是 v11.63

Go audit-backed Swarm verification already checks declared dependency steps, upstream artifact manifests, upstream receipt digests, duplicate step receipts, NUL-bearing identities, and ordered close proofs. The dependency lists still used broad helpers that silently filtered malformed `after` or `input_artifacts` entries, so a signed receipt could claim malformed dependency data that the verifier never counted.

## v11.63 不新增

- 不实现 dynamic Swarm decomposition。
- 不实现 scheduler-owned routing。
- 不实现 parallel Swarm execution。
- 不实现 cross-Zone Swarm execution。
- 不实现 Node audit-backed Swarm completeness verification。
- 不实现 generic Swarm schema validation。
- 不实现 A2A/ARD compatibility。

## 验收

```bash
go test ./cmd/go-fed-discovery -run TestVerifyAuditRejectsSwarmInputArtifactMismatch -count=1
node --test --test-concurrency=1 test/docs-contract.test.mjs
go test ./...
node --test --test-concurrency=1 test/*.test.mjs
git diff --check
gofmt -l .
```
