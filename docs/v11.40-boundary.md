# Agent Space v11.40 Boundary

状态：complete
目标：Make Node agent resolution reject missing registry context before entry lookup.

## v11.40 新增

- Node `resolveAgent` rejects missing or non-Map-like registry contexts with `registry missing`.
- The focused registry tests prove missing registry context no longer leaks JavaScript `TypeError` failures.

## 为什么这是 v11.40

`resolveAgent` sits on the local identity resolution trust path used by registry lookup, task-open requester binding, and receipt worker verification. Missing registry context previously reached `.get` and leaked a low-level JavaScript `TypeError`. v11.40 closes that context boundary without changing alias resolution semantics, registry ownership, lifecycle, or schema claims.

## v11.40 不新增

- 不改变 alias resolution semantics。
- 不改变 registry file shape。
- 不实现 registry ownership behavior。
- 不实现 registry lifecycle APIs。
- 不实现 generic registry schema validation。
- 不改变 Go verifier behavior。
- 不实现 scheduler-owned routing。
- 不实现 dynamic Swarm decomposition。
- 不实现 A2A/ARD compatibility。

## 验收

```bash
node --test --test-concurrency=1 zone-registry.test.mjs
node --test --test-concurrency=1 docs-contract.test.mjs
node --test --test-concurrency=1 *.test.mjs
go test ./...
git diff --check
gofmt -l .
```
