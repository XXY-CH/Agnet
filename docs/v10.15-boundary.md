# Agent Space v10.15 Boundary

状态：complete
目标：Make Swarm close proof order match same-audit completion order.

## v10.15 新增

- `--verify-audit` now preserves same-audit Swarm step completion order while scanning receipts.
- `--verify-audit` rejects `go_swarm_close.step_receipts` whose order differs from completed Swarm receipt order.
- Existing close proof completeness, duplicate-step, signature, task id, and receipt digest checks still apply.
- `cmd/go-fed-discovery/main_test.go` proves a reversed close summary is rejected.

## 为什么这是 v10.15

v10.14 made close summaries complete. v10.15 closes the remaining ordered-proof gap: a complete close summary must preserve execution/audit order, not merely contain the right set.

## v10.15 不新增

- 不做 dynamic Swarm decomposition。
- 不做 scheduler-owned DAG execution。
- 不做 parallel Swarm execution。
- 不做 conflict/merge receipts。
- 不做 cross-Zone Swarm。
- 不做 Swarm UI。
- 不做 receipt store/search。
- 不做 A2A/ANP/AGNTCY compatibility。

## 验收

```bash
go test ./cmd/go-fed-discovery -run TestVerifyAuditRejectsSwarmInputArtifactMismatch -count=1
node --test --test-concurrency=1 test/go-fed-discovery.test.mjs
go test ./...
node --test --test-concurrency=1 test/*.test.mjs
git diff --check
```

The focused Go test must fail before `--verify-audit` rejects reversed `go_swarm_close.step_receipts`.
