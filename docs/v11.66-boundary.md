# Agent Space v11.66 Boundary

状态：complete
目标：Make Go receipt checkpoint verification reject malformed checkpoint evidence list entries instead of silently filtering them.

## v11.66 新增

- Go `verifyCheckpoints` rejects non-string entries in signed receipt `checkpoint_refs` lists with `checkpoint ref invalid`.
- Go `verifyCheckpoints` rejects non-object entries in signed receipt `checkpoints` lists with `checkpoint invalid`.
- Existing checkpoint count, task binding, parent chain, and worker signature checks stay unchanged.
- The fix stays on the signed receipt checkpoint evidence surface: checkpoint lists are validated before count, parent, and signature checks, but this slice does not add generic receipt schema validation.

## 为什么这是 v11.66

Go receipt verification already checks checkpoint count, task binding, parent links, and worker signatures. The list parsing still used broad helper functions, so malformed signed checkpoint evidence entries could be silently filtered before those checks ran.

## v11.66 不新增

- 不实现 model KV/cache restore。
- 不实现 task migration。
- 不实现 scheduler-owned checkpoint routing。
- 不改变 resume semantics。
- 不实现 generic receipt schema validation。
- 不实现 A2A/ARD compatibility。

## 验收

```bash
go test ./cmd/go-fed-discovery -run TestVerifyCheckpointsRejectsMalformedLists -count=1
node --test --test-concurrency=1 docs-contract.test.mjs
go test ./...
node --test --test-concurrency=1 *.test.mjs
git diff --check
gofmt -l .
```
